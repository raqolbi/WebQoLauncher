services:
  app:
    image: nginx:1.27-alpine
    container_name: webqo-app-inventory
    ports:
      - "${PORT_APP}:80"
    volumes:
      - ./public:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
