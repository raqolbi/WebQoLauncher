services:
  app:
    image: nginx:1.27-alpine
    container_name: webqo-app-dashboard
    ports:
      - "${PORT_APP}:80"
    volumes:
      - ./public:/usr/share/nginx/html:ro
    restart: unless-stopped
